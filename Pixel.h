class Pixel {
    private:
        unsigned int r,g,b;

    public:
        Pixel (); //Constructeur par défaut de la classe: initialise le pixel à la couleur noire

        Pixel (nr,ng,nb); //Constructeur par défaut de la classe: initialise le pixel à la couleur noire

        ~Pixel ();


}